image: $CI_REGISTRY_IMAGE/builder

stages:
  - prebuild
  - build
  - lint
  - test
  - integration
  - staging

variables:
  DOCKER_DRIVER: overlay2

include:
  - local: ci/base/prebuild.yml
  - local: ci/lark/build.yml
  - local: ci/lark/code-quality.yml
  - local: ci/lark/dependency-scanning.yml
  - local: ci/lark/lint.yml
  - local: ci/lark/test.yml
  - local: ci/lark/staging.yml
  - local: ci/starlight/build.yml
  - local: ci/starlight/code-quality.yml
  - local: ci/starlight/dependency-scanning.yml
  - local: ci/starlight/lint.yml
  - local: ci/starlight/test.yml

# ---------------------------------------------------------------------------

.auto_devops: &auto_devops |
  # helper functions for CI tasks
  [[ "$TRACE" ]] && set -x
  # Extract "MAJOR.MINOR" from CI_SERVER_VERSION and generate "MAJOR-MINOR-stable" for Security Products
  export SP_VERSION=$(echo "$CI_SERVER_VERSION" | sed 's/^\([0-9]*\)\.\([0-9]*\).*/\1-\2-stable/')

  function registry_login() {
    if [[ -n "$CI_REGISTRY_USER" ]]; then
      echo "Logging to GitLab Container Registry with CI credentials..."
      docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
      echo ""
    fi
  }

  function setup_docker() {
    if ! docker info &>/dev/null; then
      if [ -z "$DOCKER_HOST" ]; then
        export DOCKER_HOST='tcp://localhost:2375'
      fi
    fi
  }

  function code_quality() {
    docker run --env SOURCE_CODE="$PWD" \
               --volume "$PWD":/code \
               --volume /var/run/docker.sock:/var/run/docker.sock \
               "registry.gitlab.com/gitlab-org/security-products/codequality:$SP_VERSION" /code/$1
  }

  function dependency_scanning() {
    case "$CI_SERVER_VERSION" in
      *-ee)
        docker run --env DEP_SCAN_DISABLE_REMOTE_CHECKS="${DEP_SCAN_DISABLE_REMOTE_CHECKS:-false}" \
                   --volume "$PWD:/code" \
                   --volume /var/run/docker.sock:/var/run/docker.sock \
                   "registry.gitlab.com/gitlab-org/security-products/dependency-scanning:$SP_VERSION" /code
        ;;
      *)
        echo "GitLab EE is required"
        ;;
    esac
  }

before_script:
  - *auto_devops
  - apk update && apk add git
  - git --version
