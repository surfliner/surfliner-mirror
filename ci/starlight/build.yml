starlight-build:
  extends:
    - .kaniko-build
    - .only-refs-default
  script:
    - build_image_for "starlight" "starlight_web"
  only:
    changes:
      - .gitlab-ci.yml
      - ci/starlight/**/*
      - starlight/Dockerfile
      - starlight/**/*
