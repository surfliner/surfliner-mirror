starlight-lint:
  stage: lint
  image: $CI_REGISTRY_IMAGE/starlight_web:latest
  script:
    - cd starlight
    - bundle exec rubocop
  extends: .only-refs-default
  only:
    changes:
      - .gitlab-ci.yml
      - .styles/**/*
      - starlight/**/*
