ingress:
  enabled: true
  hosts:
    - host: tidewater-staging.eks.dld.library.ucsb.edu
      paths:
        - path: '/'

postgresql:
  enabled: true

extraEnvVars:
  - name: "RAILS_LOG_TO_STDOUT"
    value: "true"
extraEnvFrom:
  - secretRef:
      name: tidewater-postgres
