persistence:
  enabled: false
consumer:
  rabbitmq:
    host: surfliner-rabbitmq-review.rabbitmq-review.svc
postgresql:
  persistence:
    enabled: false
tidewater:
  auth:
    method: developer
  sitemaps:
    enabled: false
  sample_data: true
superskunk:
  mock_server:
    url: "http://mmock:8083"

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 20m
