persistence:
  enabled: false
postgresql:
  persistence:
    enabled: false
tidewater:
  auth:
    method: developer
  sitemaps:
    enabled: false
  sample_data: true

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 20m
