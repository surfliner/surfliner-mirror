ingress:
  enabled: true
  hosts:
    - tidewater-staging.eks.dld.library.ucsb.edu
postgresql:
  enabled: true

consumer:
  enabled: false

extraEnvVars:
  - name: "RAILS_LOG_TO_STDOUT"
    value: "true"
extraEnvFrom:
  - secretRef:
      name: tidewater-postgres
