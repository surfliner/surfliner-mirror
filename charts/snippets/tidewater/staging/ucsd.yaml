ingress:
  enabled: true
  hosts:
    - lib-tidewater-staging.ucsd.edu

consumer:
  enabled: false

postgresql:
  enabled: true

extraEnvVars:
  - name: "RAILS_LOG_TO_STDOUT"
    value: "true"
extraEnvFrom:
  - secretRef:
      name: tidewater-postgres
