ingress:
  enabled: true
  hosts:
    - host: lib-tidewater-staging.ucsd.edu
      paths:
        - path: '/'

postgresql:
  enabled: true

extraEnvVars:
  - name: "RAILS_LOG_TO_STDOUT"
    value: "true"
extraEnvFrom:
  - secretRef:
      name: tidewater-postgres
