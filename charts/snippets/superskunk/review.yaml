consumers:
  defaultKeyEnabled: true
  keys:
    - name: tidewater
      secretName: superskunk-default-tidewater-keypair

postgresql:
  primary:
    persistence:
      enabled: false
  readReplicas:
    persistence:
      enabled: false
