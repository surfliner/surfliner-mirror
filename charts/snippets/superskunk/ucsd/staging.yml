superskunk:
  db:
    metadata_database_name: comet_metadata
  metadata_models:
    - ucsd-metadata
  comet_base: https://lib-comet-staging.ucsd.edu

ingress:
  enabled: false

postgresql:
  enabled: false
  auth:
    username: comet
  postgresqlHostname: lib-psql-stage.ucsd.edu

serviceAccount:
  name: superskunk

existingSecret:
  enabled: true
