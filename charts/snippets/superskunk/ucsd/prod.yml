superskunk:
  db:
    metadata_database_name: comet_metadata
  metadata_models:
    - ucsd-metadata-sample

ingress:
  enabled: false

postgresql:
  enabled: false
  auth:
    username: comet
  postgresqlHostname: lib-psql-prod.ucsd.edu

existingSecret:
  enabled: true

serviceAccount:
  name: superskunk
