---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "{{ include "solr.schema-name" . }}"
  labels:
{{ include "solr.common.labels" . | indent 4 }}
data:
{{- if .Values.schema }}
  schema.xml: |
{{ .Values.schema | indent 4 }}
{{- else }}
  schema.xml: false
{{- end }}
