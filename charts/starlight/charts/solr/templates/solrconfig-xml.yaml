---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "{{ include "solr.solrconfig-name" . }}"
  labels:
{{ include "solr.common.labels" . | indent 4 }}
data:
{{- if .Values.solrconfig }}
  solrconfig.xml: |
{{ .Values.solrconfig | indent 4 }}
{{- else }}
  solrconfig.xml: false
{{- end }}
