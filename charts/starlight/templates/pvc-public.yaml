{{- if .Values.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "starlight.fullname" . }}-rails-public
spec:
  resources:
    requests:
      storage: {{ .Values.persistence.public.size }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.public.class }}
  storageClassName: {{ .Values.persistence.public.class }}
  {{- end }}
{{- end }}
