---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rails-public
spec:
  resources:
    requests:
      storage: {{ .Values.pvc.public.size }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.pvc.public.class }}
