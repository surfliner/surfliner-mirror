---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rails-cache
spec:
  resources:
    requests:
      storage: {{ .Values.pvc.cache.size }}
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.pvc.cache.class }}
