categories:
  - API
  - Authority
questions:
- variable: persistence_adapters.index
  label: Index Adapter Type
  description: "The backend to use for the index; use 'solr' for full search support."
  default: memory
  options:
    - memory
    - solr
  type: enum
  required: true
