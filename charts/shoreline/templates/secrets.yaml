apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shoreline.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "shoreline.name" . }}
    helm.sh/chart: {{ include "shoreline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
data:
  db-username: {{ .Values.postgresql.postgresqlUsername | b64enc | quote }}
  db-password: {{ .Values.postgresql.postgresqlPassword | b64enc | quote }}
