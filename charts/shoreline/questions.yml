questions:
- variable: service.type
  default: "ClusterIP"
  description: "Service type; see https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types"
  label: Service Type
  type: enum
  group: "Services and Load Balancing"
  options:
    - "ClusterIP"
    - "NodePort"
  required: true
- variable: ingress.enabled
  default: true
  description: "Expose app using Layer 7 Load Balancer - ingress"
  type: boolean
  label: Expose app using Layer 7 Load Balancer
  show_subquestion_if: true
  group: "Services and Load Balancing"
  subquestions:
  - variable: ingress.hosts[0]
    default: shoreline.local
    description: "Hostname for the Starlight installation"
    type: hostname
    required: true
    label: Hostname
- variable: image.tag
  default: "stable"
  description: "Shoreline docker image tag"
  label: Image Tag
  type: string
  group: "Images"
- variable: theme
  default: ""
  description: "Site theme to deploy; e.g., `ucsb`"
  group: Theme
  label: Theme
  type: string
