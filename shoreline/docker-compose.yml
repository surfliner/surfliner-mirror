version: '3.7'

services:
  web:
    build:
      context: ..
      dockerfile: shoreline/Dockerfile
      target: shoreline-dev
    image: shoreline:development
    env_file:
      - .env
    volumes:
      - .:/home/shoreline/app
    depends_on:
      - db_migrate
      - postgres
      - solr
      - geoserver
    stdin_open: true
    tty: true
    ports:
     - '3000:3000'
    networks:
      - shoreline

  db_migrate:
    image: shoreline:development
    env_file:
      - .env
    entrypoint: ["sh", "-c"]
    command: db-setup.sh
    depends_on:
      - postgres
    volumes:
      - .:/home/shoreline/app
    networks:
      - shoreline

  geoserver:
    image: kartoza/geoserver:2.21.1
    volumes:
      - shoreline_geoserver_data:/data
    env_file:
      - .env
    ports:
      - '8080:8080'
    networks:
      - shoreline

  postgres:
    image: bitnami/postgresql:12.11.0-debian-11-r15
    volumes:
      - "shoreline_db:/bitnami/postgresql"
    env_file:
      - .env
    networks:
      - shoreline

  solr:
    image: bitnami/solr:8.11.2-debian-11-r6
    ports:
      - '8983:8983'
    volumes:
      - ./solr/conf:/opt/shoreline
      - shoreline_solr_data:/bitnami/solr
    env_file:
      - .env
    networks:
      - shoreline

volumes:
  shoreline_db:
  shoreline_geoserver_data:
  shoreline_solr_data:

networks:
  shoreline:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: br-shoreline
