# Tidewater API Endpoints

### GET `/oai`

This is the main OAI endpoint for Tidewater.

### GET `/item`

The `source_iri` parameter must be specified.
This endpoint redirects to an OAI record for the item with the provided
  `source_iri`.
