---
- name: remove default Apache site
  file:
    path: /etc/httpd/conf.d/welcome.conf
    state: absent
  become: yes

- name: set httpd to restart on reboot
  systemd:
    daemon_reload: yes
    enabled: yes
    name: httpd
    state: started
  become: yes
