---
# tasks file for apache-passenger

- name: Import package names to install
  include_vars: '{{ ansible_os_family }}.yml'

- name: Install packages
  package:
    name: '{{ apache_packages }}'
    state: present
  notify: Enable Apache
