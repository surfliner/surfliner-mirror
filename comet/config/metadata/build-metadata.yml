# This file gives some basic metadata useful for preventing build errors when
# just precompiling assets, for example.
#
# Ideally this wouldn’t be required, but right now Comet has a hard dependency
# on the +GenericObject+ class being defined.

m3_version: "1.0.beta1"

classes:
  - name: generic_object
